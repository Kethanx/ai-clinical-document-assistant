apiVersion: v1
kind: Service
metadata:
  name: cardiology-frontend-service
  namespace: cardiology-assistant
spec:
  type: LoadBalancer
  selector:
    app: cardiology-frontend
  ports:
    - port: 80
      targetPort: 8501
